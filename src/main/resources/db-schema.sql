create table hotel (
    id integer not null,
    descricao varchar(255),
    name varchar(255),
    primary key (id)
) engine=InnoDB;
    create table hotel (
        id integer not null,
        descricao varchar(255),
        name varchar(255),
        primary key (id)
    ) engine=InnoDB;

    create table hotel (
        id integer not null,
        descricao varchar(255),
        name varchar(255),
        primary key (id)
    ) engine=InnoDB;
